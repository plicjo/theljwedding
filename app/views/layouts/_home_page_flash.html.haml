- flash.each do |key, value|
  .flash{class: "alert-home-page fade in alert-#{key}"}
    %button.close{'data-dismiss' => 'alert', type: 'button'}
      %span{'aria-hidden' => 'true'} x
      %span.sr-only Close
    %strong= value.html_safe
